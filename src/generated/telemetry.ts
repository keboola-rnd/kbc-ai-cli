// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../context';

export function registerTelemetryCommands(parent: Command, ctx: CliContext) {
  const service = parent.command('telemetry').description('Telemetry API — provisioning and workspace credentials');

  const group_provisioning = service.command('provisioning').description('Telemetry provisioning operations');

  group_provisioning.command('create-credentials')
    .description('Create telemetry workspace credentials [POST]')
    .option('--data <value>', 'Credentials request as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = (await ctx.telemetryApi()).provisioning;
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('createCredentials', 'POST', '/provisioning/workspace', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
