// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../context';

export function registerSyncActionsCommands(parent: Command, ctx: CliContext) {
  const service = parent.command('sync-actions').description('Sync Actions API — git repository operations');

  service.command('enroll-mfa')
    .description('Enroll MFA for a user [POST]')
    .option('--data <value>', 'MFA enrollment data as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = await ctx.syncActionsApi();
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('enrollMFA', 'POST', '/actions', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  const group_gitRepository = service.command('git-repo').description('Git repository operations');

  group_gitRepository.command('data-app-public')
    .description('Get data app public git repository info [POST]')
    .option('--data <value>', 'Repository URL and options as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = (await ctx.syncActionsApi()).gitRepository;
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('getDataAppPublicGitRepository', 'POST', '/actions', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  group_gitRepository.command('data-app-private-password')
    .description('Get data app private git repository (password auth) [POST]')
    .option('--data <value>', 'Repository URL, username, password as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = (await ctx.syncActionsApi()).gitRepository;
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('getDataAppPasswordPrivateGitRepository', 'POST', '/actions', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  group_gitRepository.command('data-app-private-ssh')
    .description('Get data app private git repository (SSH key auth) [POST]')
    .option('--data <value>', 'Repository URL, username, SSH key as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = (await ctx.syncActionsApi()).gitRepository;
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('getDataAppSSHKeyPrivateGitRepository', 'POST', '/actions', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  group_gitRepository.command('public')
    .description('Get public git repository info [POST]')
    .option('--data <value>', 'Repository URL and options as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = (await ctx.syncActionsApi()).gitRepository;
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('getPublicGitRepository', 'POST', '/actions', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  group_gitRepository.command('private')
    .description('Get private git repository info (password auth) [POST]')
    .option('--data <value>', 'Repository URL, username, password as JSON (required)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = (await ctx.syncActionsApi()).gitRepository;
        const bodyData = opts.data ? JSON.parse(opts.data) : {};
        const result = await api._call('getPrivateGitRepository', 'POST', '/actions', 0, bodyData);
        ctx.output(result);
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
