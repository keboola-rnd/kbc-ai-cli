// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerFlowUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('flow').description('Manage flows (orchestrations)');

  unit.command('list')
    .description('List all flows in the project')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('getConfigurations', 'GET', '/branch/{branchId}/components/{0}/configs', 1, "keboola.orchestrator", { branchId: opts['branch-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No flows found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Description': String(item['description'] ?? '').slice(0, 60), 'Version': item['version'] ?? '', 'Created': item['created'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('show <flow-id>')
    .description('Show flow details (phases, tasks, schedule)')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (flowId, opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('getConfiguration', 'GET', '/branch/{branchId}/components/{0}/configs/{1}', 2, "keboola.orchestrator", flowId, { branchId: opts['branch-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
