// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerTransformationUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('transformation').description('Manage transformations (SQL, Python, R)');

  unit.command('list')
    .description('List all transformations in the project')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('getComponents', 'GET', '/branch/{branchId}/components', 0, { branchId: opts['branch-id'], componentType: "transformation" });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const components = Array.isArray(result) ? result : [];
        const items: any[] = [];
        for (const comp of components) {
          const nested = comp['configurations'];
          if (Array.isArray(nested)) {
            for (const item of nested) items.push(item);
          }
        }
        if (items.length === 0) { console.log('No transformations found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Description': String(item['description'] ?? '').slice(0, 50), 'Version': item['version'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
