// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerTableUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('table').description('Manage storage tables');

  unit.command('list')
    .description('List tables in a bucket')
    .option('--bucket <value>', 'Filter by bucket ID')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().tables;
        const result = await api._call('getTables', 'GET', '/branch/default/tables', 0, { bucket: opts['bucket'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No tables found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Primary Key': item['primaryKey'] ?? '', 'Rows': item['rowsCount'] ?? '', 'Size(B)': item['dataSizeBytes'] ?? '', 'Last Import': item['lastImportDate'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('show <table-id>')
    .description('Show table details (columns, metadata)')
    .option('--json', 'Output raw JSON')
    .action(async (tableId, opts) => {
      try {
        const api: any = ctx.storageApi().tables;
        const result = await api._call('getTable', 'GET', '/branch/default/tables/{0}', 1, tableId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('preview <table-id>')
    .description('Preview table data')
    .option('--limit <value>', 'Max rows', "100")
    .option('--format <value>', 'Output format (json)', "json")
    .option('--json', 'Output raw JSON')
    .action(async (tableId, opts) => {
      try {
        const api: any = ctx.storageApi().tables;
        const result = await api._call('getDataPreview', 'GET', '/branch/default/tables/{0}/data-preview', 1, tableId, { limit: opts['limit'], format: opts['format'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
