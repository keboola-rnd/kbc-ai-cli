// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerFileUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('file').description('Manage file uploads');

  unit.command('list')
    .description('List recent file uploads')
    .option('--limit <value>', 'Max number of results', "50")
    .option('--tags <value>', 'Filter by tags (comma-separated)')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().files;
        const result = await api._call('getFiles', 'GET', '/files', 0, { limit: opts['limit'], tags: opts['tags'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No files found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Size(B)': item['sizeBytes'] ?? '', 'Public': item['isPublic'] ?? '', 'Tags': item['tags'] ?? '', 'Created': item['created'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('delete <file-id>')
    .description('Delete a file')
    .option('--json', 'Output raw JSON')
    .action(async (fileId, opts) => {
      try {
        const api: any = ctx.storageApi().files;
        const result = await api._call('deleteFile', 'DELETE', '/files/{0}', 1, fileId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
