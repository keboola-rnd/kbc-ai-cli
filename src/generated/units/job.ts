// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerJobUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('job').description('Manage jobs (run, list, monitor)');

  unit.command('list')
    .description('List recent jobs')
    .option('--status <value>', 'Filter by status (created, waiting, processing, success, error, cancelled, terminated, warning)')
    .option('--component <value>', 'Filter by component ID')
    .option('--config <value>', 'Filter by configuration ID')
    .option('--limit <value>', 'Max number of results', "50")
    .option('--offset <value>', 'Skip first N results')
    .option('--sort-by <value>', 'Sort field (id, startTime, endTime, durationSeconds)', "id")
    .option('--sort-order <value>', 'Sort order (asc, desc)', "desc")
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = await ctx.queueApi();
        const result = await api._call('searchJobs', 'GET', '/search/jobs', 0, { status: opts['status'], component: opts['component'], config: opts['config'], limit: opts['limit'], offset: opts['offset'], sortBy: opts['sortBy'], sortOrder: opts['sortOrder'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No jobs found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Status': item['status'] ?? '', 'Component': item['component'] ?? '', 'Config': item['config'] ?? '', 'Started': item['startTime'] ?? '', 'Ended': item['endTime'] ?? '', 'Duration(s)': item['durationSeconds'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('show <job-id>')
    .description('Show job details (status, results, metrics)')
    .option('--json', 'Output raw JSON')
    .action(async (jobId, opts) => {
      try {
        const api: any = await ctx.queueApi();
        const result = await api._call('getJob', 'GET', '/jobs/{0}', 1, jobId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
