// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerDataAppUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('data-app').description('Manage Data Apps');

  unit.command('list')
    .description('List all Data Apps in the project')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = await ctx.dataScienceApi();
        const result = await api._call('getApps', 'GET', '/apps', 0);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No Data Apps found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Version': item['version'] ?? '', 'Status': item['status'] ?? '', 'Created': item['createdAt'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('show <app-id>')
    .description('Show Data App details')
    .option('--json', 'Output raw JSON')
    .action(async (appId, opts) => {
      try {
        const api: any = await ctx.dataScienceApi();
        const result = await api._call('getApp', 'GET', '/apps/{0}', 1, appId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('create')
    .description('Create a new Data App')
    .requiredOption('--name <value>', 'App name')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = await ctx.dataScienceApi();
        const result = await api._call('createApp', 'POST', '/apps', 0, { name: opts['name'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('delete <app-id>')
    .description('Delete a Data App')
    .option('--json', 'Output raw JSON')
    .action(async (appId, opts) => {
      try {
        const api: any = await ctx.dataScienceApi();
        const result = await api._call('deleteApp', 'DELETE', '/apps/{0}', 1, appId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('logs <app-id>')
    .description('Get Data App logs (tail)')
    .option('--json', 'Output raw JSON')
    .action(async (appId, opts) => {
      try {
        const api: any = await ctx.dataScienceApi();
        const result = await api._call('getAppLogsTail', 'GET', '/apps/{0}/logs/tail', 1, appId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('runs <app-id>')
    .description('List Data App deployment runs')
    .option('--json', 'Output raw JSON')
    .action(async (appId, opts) => {
      try {
        const api: any = await ctx.dataScienceApi();
        const result = await api._call('getAppRuns', 'GET', '/apps/{0}/runs', 1, appId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No runs found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Status': item['status'] ?? '', 'Created': item['createdAt'] ?? '', 'Finished': item['finishedAt'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
