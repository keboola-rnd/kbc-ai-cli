// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerConfigUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('config').description('Manage component configurations');

  unit.command('list <component-id>')
    .description('List configurations for a component')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (componentId, opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('getConfigurations', 'GET', '/branch/{branchId}/components/{0}/configs', 1, componentId, { branchId: opts['branch-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No configurations found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Description': String(item['description'] ?? '').slice(0, 60), 'Version': item['version'] ?? '', 'Created': item['created'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('show <component-id> <config-id>')
    .description('Show configuration details')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (componentId, configId, opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('getConfiguration', 'GET', '/branch/{branchId}/components/{0}/configs/{1}', 2, componentId, configId, { branchId: opts['branch-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('create <component-id>')
    .description('Create a new configuration')
    .requiredOption('--name <value>', 'Configuration name')
    .option('--description <value>', 'Configuration description')
    .option('--data <value>', 'Configuration JSON data')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (componentId, opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('createConfiguration', 'POST', '/branch/{branchId}/components/{0}/configs', 1, componentId, { name: opts['name'], description: opts['description'], data: opts['data'], branchId: opts['branch-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('delete <component-id> <config-id>')
    .description('Delete a configuration')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--json', 'Output raw JSON')
    .action(async (componentId, configId, opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('deleteConfiguration', 'DELETE', '/branch/{branchId}/components/{0}/configs/{1}', 2, componentId, configId, { branchId: opts['branch-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('search')
    .description('Search configurations across all components')
    .option('--branch-id <value>', 'Branch ID', "default")
    .option('--component-type <value>', 'Filter by component type')
    .option('--configuration-name <value>', 'Filter by configuration name')
    .option('--configuration-id <value>', 'Filter by configuration ID')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().componentsAndConfigurations;
        const result = await api._call('searchComponentConfigurations', 'GET', '/branch/{branchId}/search/component-configurations', 0, { branchId: opts['branch-id'], componentType: opts['component-type'], configurationName: opts['configuration-name'], configurationId: opts['configuration-id'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No configurations found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Description': String(item['description'] ?? '').slice(0, 50), 'Component': item['componentId'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
