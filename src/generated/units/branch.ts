// AUTO-GENERATED by generator/generate.ts — DO NOT EDIT
// Re-generate with: make generate

import { Command } from 'commander';
import type { CliContext } from '../../context';
import { formatTable } from '../../output';

export function registerBranchUnit(parent: Command, ctx: CliContext) {
  const unit = parent.command('branch').description('Manage development branches');

  unit.command('list')
    .description('List all branches')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().branches;
        const result = await api._call('getDevBranches', 'GET', '/branch', 0);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        const items = Array.isArray(result) ? result : [];
        if (items.length === 0) { console.log('No branches found.'); return; }
        const rows = items.map((item: any) => ({ 'ID': item['id'] ?? '', 'Name': item['name'] ?? '', 'Default': item['isDefault'] ?? '', 'Created': item['created'] ?? '' }));
        console.log(formatTable(rows));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('create')
    .description('Create a new development branch')
    .requiredOption('--name <value>', 'Branch name')
    .option('--description <value>', 'Branch description')
    .option('--json', 'Output raw JSON')
    .action(async (opts) => {
      try {
        const api: any = ctx.storageApi().branches;
        const result = await api._call('createDevBranchJob', 'POST', '/branch', 0, { name: opts['name'], description: opts['description'] });
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

  unit.command('delete <branch-id>')
    .description('Delete a development branch')
    .option('--json', 'Output raw JSON')
    .action(async (branchId, opts) => {
      try {
        const api: any = ctx.storageApi().branches;
        const result = await api._call('deleteDevBranchJob', 'DELETE', '/branch/{0}', 1, branchId);
        if (opts.json) { console.log(JSON.stringify(result, null, 2)); return; }
        console.log(JSON.stringify(result, null, 2));
      } catch (error: unknown) {
        ctx.handleError(error);
      }
    });

}
